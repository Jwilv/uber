import { View, Text } from 'react-native'
import React from 'react'

export default function DriversSelection() {
    return (
        <View
            style={{
                width: '100%',
                height: '100%',
                display: 'flex',
                backgroundColor: 'red',
                alignItems: 'center',
            }}>
            <Text>DriversSelection</Text>
        </View>
    )
}